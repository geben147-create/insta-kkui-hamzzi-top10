# 작업기록

## 2026-09-23 (세션: 유튜브 99개 자막 추출 + 인스타 @ggooiikim TOP10 자막 + TOP3 정밀 분석)

### 한 일
1. 유튜브 99개 자막 추출 (yt-dlp, 한국어 우선 → 영어/일본어/원어) → `subs/ALL_subtitles.txt`, `subs/txt/`
   - 92개 자막 확보 + 자막 없는 중국어 대화 영상(qJVW8kpTtY0) 1개는 Whisper small로 음성→자막 생성
   - 못 받은 것: 멤버 전용 1개(6wSc6z5S7Z0), 음악/믹스라 자막 자체가 없는 5개(tJsWUHqcTSM, BcbmFxbdsJ0, lLz9_XdyJGo, aoUX55g5iK0, xbc6eDHmXkE)
2. 인스타 @ggooiikim(김꾸이) 릴스 42개 수집 → 점수(하루 평균 조회·좋아요·댓글, 댓글 1.5배) → TOP10 자막
   - `insta/인스타 꾸이 결과/00_복사용_TOP10_순위순.txt` (사용자가 폴더명을 '결과'→'인스타 꾸이 결과'로 변경함)
   - 1~3위는 프레임 확인으로 한·영·일 자막 전 줄 검증 완료
3. TOP3 정밀 분석 (watch 스킬 + 자체 프레임 분석 + Whisper medium + OCR 위치 + 스펙트럼)
   - `insta/report/rank1.html`, `rank2.html`, `rank3.html`, `index.html` (한 페이지 = 한 영상)
   - `insta/kit/edl_render.py` (편집표 → FFmpeg 자동 편집), `edl_to_hyperframes.py` (편집표 → HyperFrames 컴포지션)
   - 편집표: `kit/edl_rank1_onetake.json`, `edl_rank1_3clips.json`, `edl_rank2_chuseok.json`, `edl_rank3_burger.json`

### 실행한 주요 명령
- `yt-dlp --skip-download --write-subs --write-auto-subs --sub-langs ...` (4병렬 + 재시도)
- `python analyze_video.py <id>` (프레임 차이로 컷·전환 검출), `python analyze_audio.py <id>` (LUFS·온셋·Whisper)
- `powershell ocr_bbox.ps1` (Windows 한국어 OCR + 위치)
- `python kit/test_render.py edl_*.json` / `python kit/check_colors.py ...` (더미 클립 렌더 검증)
- `python kit/build_pages.py`

### 테스트 결과
- FFmpeg 편집표 4개 모두 렌더 성공, 길이 정확 (12.04 / 12.04 / 44.50 / 22.40초)
- 색상 검사 105곳 중 104곳 일치 (나머지 1곳은 흰 플래시 구간 = 정상)
- 3줄 자막(한·영·일)·♬ 기호 렌더 확인
- HTML: 2위 19샷·프롬프트 44개, 3위 13샷·33개, 콘솔 에러 0, 깨진 이미지 0, 동물 교체 시 캐릭터 프롬프트 21개 자동 치환 확인

### 실패·주의점
- watch 스킬: 최신 FFmpeg에서 `-vsync` 옵션 제거로 실패 → 작업 폴더에 복사본만 `-fps_mode`로 고쳐 사용 (플러그인 원본은 안 건드림)
- watch 스킬의 OpenAI 키가 무효(401) → 로컬 faster-whisper로 대체. 🔴 키 교체는 사용자가 직접
- 인스타 API 429(요청 제한) → 계정 보호를 위해 화면 스크롤·로그아웃 yt-dlp만 사용
- HyperFrames CLI 미설치 → `npx hyperframes check` 미실행 (자체 규칙 검사만 통과)
- 이 폴더는 세션 전용 임시 폴더 → 보관하려면 다른 폴더로 이동 필요

### 다음 행동
- 바꿀 동물 결정 → 캐릭터 시트 이미지 1장 생성(크레딧 9~18)부터 시작
- 결과물을 영구 폴더로 이동 (예: `○캐릭폴로ai영상생성`)

## 2026-09-24 (4~10위 분석 추가 + 깃허브 공개)
- 4~10위 7개 분석 페이지 추가 (report/rank4~10.html), 편집표 edl_rank4~10.json, HyperFrames 7개
- 검증: 더미 렌더 길이 7/7 정확, 색상 검사 189/189 일치, 페이지 10개 200 응답·깨진 이미지 0
- 포트 8765를 다른 세션이 사용 → 이 작업 서버는 18791로 분리
- 깃허브 저장소 이름 변경 ggooiikim-top3-analysis → insta-kkui-hamzzi-top10, 사용자 요청으로 공개 전환 + GitHub Pages
- 웹 주소: https://geben147-create.github.io/insta-kkui-hamzzi-top10/ (전 페이지 200 확인)

## 2026-09-26 (11~17위 7개 추가)
- 랭킹 11~17위(Dazoyz_CHwN, Dc0bTviiDlX, Dcnot5XCXqM, DbVI4YICk0g, DbpJNIDzseF, DdTVlOUiOmo, Db5Nb5px2d_) 분석 페이지 추가
- 검증: 더미 렌더 길이 7/7 정확, 색상 검사 171/171 (15위 장면 사이 0.2초 틈 발견·수정 후 통과)
